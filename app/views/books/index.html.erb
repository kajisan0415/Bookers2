<%= render 'users/list', user: current_user %>

 <% if flash[:notice] %>
  <div class="flash">
  <%= flash[:notice] %>
  </div>
 <% end %>

 <h1>Books</h1>
  <table>
   <thead>
   <th>Title</th>
   <th>Opinion</th>
   <th colspan="3"></th>
   </thead>
  <tbody>
    <% @books.each do |book| %>
    <tr>
      <td><%= link_to "/books/#{book.id}" do %>
      <%= book.title %>
      <% end %>
      </td>
      <td><%= book.body %></td>
    </tr>
     <% end %>
   </tbody>
  </table>



